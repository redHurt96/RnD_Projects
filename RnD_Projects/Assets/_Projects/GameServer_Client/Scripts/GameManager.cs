using Shared;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    private void Start()
    {
        var player = new Player();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
